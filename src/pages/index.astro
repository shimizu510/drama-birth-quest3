---
import {
  buildAbsoluteUrl,
  formatDateLabel,
  getQuestDescription,
  getQuestSubtitle,
  normalizeQuest,
} from '../lib/quests';

const site = Astro.site;
const canonical = buildAbsoluteUrl('/', site);
const appDownloadURL = '#download-app';
const questModules = import.meta.glob('../data/quests/*.json', { eager: true });

const quests = Object.entries(questModules)
  .map(([filePath, module]) => normalizeQuest(filePath, module))
  .sort((left, right) => {
    const leftTime = left.updatedAt ? new Date(left.updatedAt).getTime() : 0;
    const rightTime = right.updatedAt ? new Date(right.updatedAt).getTime() : 0;
    return rightTime - leftTime;
  });

const pageTitle = 'Drama Birth Quest Archive';
const pageDescription =
  '公開中のクエスト一覧です。各クエストの固定ページからあらすじを確認し、アプリ公開後はそのままダウンロード導線へ進めます。';
---
<!doctype html>
<html lang="ja">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{pageTitle}</title>
    <meta name="description" content={pageDescription} />
    <meta name="google-site-verification" content="43r4xc_D4boZaBpwXqiFqyuuf29Q-2BJGERdXTmvXVk" />
    <link rel="canonical" href={canonical} />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={pageDescription} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonical} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={pageTitle} />
    <meta name="twitter:description" content={pageDescription} />
    <style>
      :root {
        color-scheme: light;
        --bg: #f6f3eb;
        --surface: rgba(255, 255, 255, 0.84);
        --surface-strong: #fffdf8;
        --text: #1f2937;
        --muted: #5b6472;
        --line: rgba(31, 41, 55, 0.12);
        --accent: #c75a2c;
        --accent-strong: #8c3412;
        --shadow: 0 20px 50px rgba(55, 37, 15, 0.12);
      }

      * {
        box-sizing: border-box;
      }

      body {
        margin: 0;
        font-family: "Hiragino Sans", "Noto Sans JP", sans-serif;
        color: var(--text);
        background:
          radial-gradient(circle at top, rgba(199, 90, 44, 0.18), transparent 34%),
          linear-gradient(180deg, #f1ebde 0%, var(--bg) 42%, #f8f7f3 100%);
      }

      a {
        color: inherit;
      }

      .shell {
        width: min(1100px, calc(100% - 32px));
        margin: 0 auto;
        padding: 24px 0 64px;
      }

      .hero {
        padding: 28px;
        border: 1px solid var(--line);
        border-radius: 28px;
        background:
          linear-gradient(135deg, rgba(255, 255, 255, 0.92), rgba(251, 245, 232, 0.86)),
          var(--surface);
        box-shadow: var(--shadow);
      }

      .eyebrow {
        margin: 0 0 10px;
        color: var(--accent-strong);
        font-size: 0.85rem;
        font-weight: 700;
        letter-spacing: 0.08em;
        text-transform: uppercase;
      }

      h1 {
        margin: 0;
        font-size: clamp(2.2rem, 5vw, 4rem);
        line-height: 1.05;
      }

      .lead {
        max-width: 760px;
        margin: 18px 0 0;
        color: var(--muted);
        font-size: 1.05rem;
        line-height: 1.8;
      }

      .hero-actions {
        display: flex;
        flex-wrap: wrap;
        gap: 12px;
        margin-top: 24px;
      }

      .button {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        min-height: 48px;
        padding: 0 18px;
        border-radius: 999px;
        text-decoration: none;
        font-weight: 700;
      }

      .button-primary {
        background: linear-gradient(135deg, var(--accent), var(--accent-strong));
        color: white;
      }

      .button-secondary {
        border: 1px solid var(--line);
        background: rgba(255, 255, 255, 0.7);
      }

      .stats {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
        gap: 14px;
        margin-top: 28px;
      }

      .stat {
        padding: 18px;
        border: 1px solid var(--line);
        border-radius: 22px;
        background: rgba(255, 255, 255, 0.64);
      }

      .stat-label {
        color: var(--muted);
        font-size: 0.88rem;
      }

      .stat-value {
        display: block;
        margin-top: 8px;
        font-size: 1.5rem;
        font-weight: 800;
      }

      .section {
        margin-top: 28px;
      }

      .section-header {
        display: flex;
        justify-content: space-between;
        gap: 16px;
        align-items: end;
        margin-bottom: 16px;
      }

      .section-header h2 {
        margin: 0;
        font-size: 1.5rem;
      }

      .section-header p {
        margin: 0;
        color: var(--muted);
      }

      .quest-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
        gap: 16px;
      }

      .quest-card {
        display: flex;
        flex-direction: column;
        gap: 14px;
        padding: 22px;
        border: 1px solid var(--line);
        border-radius: 24px;
        background: var(--surface-strong);
        box-shadow: 0 12px 34px rgba(50, 36, 15, 0.08);
        text-decoration: none;
      }

      .quest-card h3 {
        margin: 0;
        font-size: 1.2rem;
      }

      .quest-meta,
      .quest-date {
        margin: 0;
        color: var(--muted);
        font-size: 0.94rem;
        line-height: 1.6;
      }

      .quest-summary {
        margin: 0;
        line-height: 1.8;
      }

      .quest-link {
        margin-top: auto;
        color: var(--accent-strong);
        font-weight: 700;
      }

      .download {
        margin-top: 28px;
        padding: 24px;
        border: 1px solid var(--line);
        border-radius: 24px;
        background: linear-gradient(135deg, rgba(199, 90, 44, 0.1), rgba(255, 255, 255, 0.9));
      }

      .download h2,
      .download p {
        margin: 0;
      }

      .download p {
        margin-top: 10px;
        color: var(--muted);
        line-height: 1.8;
      }

      @media (max-width: 720px) {
        .shell {
          width: min(100% - 20px, 1100px);
          padding-top: 14px;
        }

        .hero,
        .quest-card,
        .download {
          border-radius: 20px;
        }
      }
    </style>
  </head>
  <body>
    <main class="shell">
      <section class="hero">
        <p class="eyebrow">Quest Discovery</p>
        <h1>物語クエストを、検索から見つけやすく。</h1>
        <p class="lead">
          Drama Birth の公開クエスト一覧です。各ページではあらすじや作者情報を確認でき、アプリ公開後はそのまま体験導線につながる構成にしています。
        </p>
        <div class="hero-actions">
          <a class="button button-primary" href={appDownloadURL}>アプリ案内を見る</a>
          <a class="button button-secondary" href="#quest-list">公開クエスト一覧へ</a>
        </div>
        <div class="stats">
          <div class="stat">
            <span class="stat-label">公開クエスト</span>
            <strong class="stat-value">{quests.length}</strong>
          </div>
          <div class="stat">
            <span class="stat-label">主な用途</span>
            <strong class="stat-value">検索流入</strong>
          </div>
          <div class="stat">
            <span class="stat-label">公開形式</span>
            <strong class="stat-value">固定URL</strong>
          </div>
        </div>
      </section>

      <section class="section" id="quest-list">
        <div class="section-header">
          <div>
            <h2>公開クエスト一覧</h2>
            <p>各クエストページは個別URLで公開され、検索エンジンから直接参照できます。</p>
          </div>
        </div>
        <div class="quest-grid">
          {quests.map((quest) => (
            <a class="quest-card" href={quest.webPagePath}>
              <div>
                <h3>{quest.title}</h3>
                {getQuestSubtitle(quest) ? <p class="quest-meta">{getQuestSubtitle(quest)}</p> : null}
              </div>
              <p class="quest-summary">{getQuestDescription(quest)}</p>
              {formatDateLabel(quest.updatedAt) ? (
                <p class="quest-date">最終更新: {formatDateLabel(quest.updatedAt)}</p>
              ) : null}
              <span class="quest-link">詳細ページを見る</span>
            </a>
          ))}
        </div>
      </section>

      <section class="download" id="download-app">
        <h2>アプリ案内</h2>
        <p>
          App Store 公開前のため、現在はクエスト紹介ページを先行公開しています。ストアURLが確定したら、この導線を各ページ上部とトップページから直接案内できる状態にしています。
        </p>
      </section>
    </main>
  </body>
</html>
